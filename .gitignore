__pycache__/
*.pyc
*.pyo
.pytest_cache/
# B1 / N2 — advisory flock files created by locked_state() and the watchdog.
# Scoped to the specific paths — unscoped *.lock would silently exclude
# poetry.lock, Cargo.lock, yarn.lock, etc.
pipeline_state.json.lock
.eigen/*.lock
